{
  "name": "bs-parse-color",
  "version": "0.1.0",
  "scripts": {
    "clean": "bsb -clean-world",
    "build": "bsb -make-world",
    "watch": "bsb -make-world -w",
    "test": "npm run build && jest --testURL http://localhost/",
    "test:watch": "npm run build && jest --testURL http://localhost/ --watch"
  },
  "keywords": [
    "BuckleScript",
    "colors",
    "parsing",
    "fun"
  ],
  "author": "theatlasroom",
  "license": "MIT",
  "devDependencies": {
    "bs-platform": "^4.0.5",
    "bsb-js": "^1.1.7"
  },
  "dependencies": {
    "bs-jest": "^0.3.2",
    "parse-color": "^1.0.0"
  }
}
